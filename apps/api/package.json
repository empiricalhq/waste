{
  "name": "@lima-garbage/api",
  "module": "src/cmd/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot --env-file=../../.env src/cmd/server.ts",
    "start": "bun src/cmd/server.ts",
    "test:dev": "bun --hot --env-file=../../.env.test src/cmd/server.ts",
    "test": "bun test-runner.ts",
    "build": "bun build src/cmd/server.ts --outdir ./dist --target bun",
    "format": "prettier --config ../../.prettierrc --ignore-path ../../.prettierignore --write ."
  },
  "dependencies": {
    "@hono-rate-limiter/cloudflare": "^0.2.2",
    "@hono/zod-validator": "^0.7.4",
    "better-auth": "^1.3.34",
    "hono": "^4.10.4",
    "pg": "^8.16.3",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "1.3.1",
    "@types/pg": "^8.15.6"
  }
}
